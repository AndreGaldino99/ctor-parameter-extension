using System.Text;
using MSExtension.Models;

namespace MSExtension.CommandsGen
{
    public static class GenInput
    {
        public static void GenerateInput(MainCodeGenerator main)
        {
            var input = new StringBuilder();
        }
    }
}
