using System.Text;
using MSExtension.Models;

namespace MSExtension.CommandsGen
{
    public static class GenOutput
    {
        public static void GenerateOutput(MainCodeGenerator main)
        {
            var output = new StringBuilder();
        }
    }
}
